# Installation (Termux and Linux)

1. `git clone <repo-url>`
2. `cd Crypto-Bot`
3. `bash scripts/setup.sh`
4. `source .venv/bin/activate`
5. `python scripts/health_check.py`

If setup fails, read the error and rerun after fixing package/network issues.
