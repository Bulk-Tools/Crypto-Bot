# Machine Learning

Run:
`python scripts/train_model.py`

The target is configurable and based on forward price movement with horizon/threshold logic.

Models currently compared:
- Logistic Regression
- Random Forest
- Gradient Boosting

Registry metadata is written to `models/registry.json`.
